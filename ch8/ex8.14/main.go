// @todo

// Change the chat server's network protocol so that each client provides its name on
// entering. Use that name instead of the network address when prefixing each message
// with its sender's identity.